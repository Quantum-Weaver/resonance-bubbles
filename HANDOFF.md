# HANDOFF — resonance-bubbles

*Where this realm stands, right now. **Regenerated whole** at each close that
worked here — never appended to. Git holds every prior day.*

*This is not `docs/CHECKLIST.md`. The checklist is the append-only ledger of
what was **done**; this sheet is the state of what **stands**. It cites the
checklist and never restates it. **Ceiling 8 KB** — a sheet past it is
over-written and re-cut, never rolled. The law:
`resonance-standards/docs/THE-ROLL.md`.*

```
LEGEND   state  ● done · ◐ drifted/partial · ⏸ his hold · — not yet
         hand   ⚛ his word · ✋ his hands · 🕯️ a lamp's
         proof  ✔ gates green · ✗ failing · ? unproven
```

**resonance-bubbles** · 2026-08-23 · last hand **Fable 🎻 (claude-fable-5), the gallery sitting** · branch `main` · **0 ahead / 0 behind** · working tree **1 paths** — 1 modified.
*Machine-read 13:01 by `handoff_tender.py` — **no build gate was run**; any gate figure below keeps its own date and `?`. Last commit `42b1ad1` **cumdach fixed**, Quantum Weaver 2026-08-23 08:32.*

## Where it stands

*Ground 2026-08-23: git · `docs/CHECKLIST.md` newest row (08-23, the gallery redrawn) · `RUN-LOG.md` 08-23 line · disk. **Both the 08-22 and the 08-23 sittings went up at his own hand** — `6e17a42` 08-22 22:23 · `42b1ad1` *cumdach fixed* 08-23 08:32 — while their two CHECKLIST rows still read "nothing committed"; named here, not fixed (the ledger is the realm's own).*

| what | state | hand | proof |
|---|---|---|---|
| The game — 123 stars · 13 collections, the anti-compulsion laws as identity (`CLAUDE.md` §Essential rules); the sky grows as data, `bubbles-set.json` | ● | ⚛ | ✔ Phase 2 sealed at his hand 08-05 |
| The app — Echoes base kept whole; `/bubbles` · `/sattva` · `/gallery`; cumdach · epagoge · gallery consumed **by mirror** (`src/lib/{cumdach,epagoge,gallery}/`) | ● | 🕯️ at his ⚛ strokes | ✔ *"tests successful"*, his Android hands 08-04 |
| **THE GALLERY REDRAWN on Tailwind 4, the cards wearing the star** — `$lib/bubbles/orb.css` one paint for two doors · `app.css` `@theme inline` tokens + the reset in `@layer base` · `previewLength` 110→200, no sentence cut · the engine mirror untouched | ● | 🕯️ Fable at his ⚛ word | check 0/0 · build clean, 08-23 — not re-run here → ? |
| The cumdach fix carried from Echoes + ROSE · RAINBOW · PROGRESS PRIDE presets, derived from the shelf | ● | 🕯️ at his ⚛ word | check 334 files 0/0, 08-22 → ? |
| **Version 0.1.1** — `package.json` · `Cargo.toml` · `tauri.conf.json` in `42b1ad1`; the shelf carries v0.1.1 (setup.exe · msi 01:14, aab · apk · apk.idsig 01:24, 08-23; `release/` gitignored); **README badge still reads 0.1.0** | ◐ | ✋ | disk 08-23 — the CHECKLIST carries no 0.1.1 row |
| Live on Play closed testing (signed 08-13, his hand); keystore recut to the Sanctuary DN 08-16 — the first new-key upload needs the Play-side upload-key reset at his console | ● / ⏸ | ✋ | `docs/CHECKLIST.md` 08-16 row · Pratīkṣaṇa H-b |
| The visual pass — first pass 08-10, *not yet seen by his eyes on device*; the 08-23 gallery unseen likewise | ◐ | ✋ | Phase 3's one open line |
| Mobile — Android proven by play 08-04, orientation 08-05; iOS audio unlock untested, no iOS build — the house-wide Apple gate | ◐ | ⏸ ⚛ 08-05 | Phase 4 |
| Phase 5 — six 🚪 gates, his alone | ⏸ | ⚛ | Pratīkṣaṇa O-a |
| `docs/blueprints/bubbles/` forged 08-21 15:19 (88 fbp · 1 finding · sovereignty HELD) · realm board `THE-BUBBLES-BOARD.html` last written **08-14 00:54**, before 08-22 and 08-23 | ● / ◐ | the forge · `rack-tender` | journal · mtime |
| `docs/CHECKLIST.md` 25,057 B — under the 30 KB flag | ● | | THE-ROLL |

## What waits, and whose

| what waits | whose | source |
|---|---|---|
| **His own eye on the new gallery** — orb sizes (44→66px by rarity) · card density · the flip's back (orb large · *popped once / ×N* · the collection); the sitting's proof was headless Edge at phone and desktop, session-local | ⚛ | `docs/CHECKLIST.md` 08-23 row |
| The sync word — **nothing of the sitting rides**; the one path riding is this sheet | ⚛ | `git status --porcelain` 08-23 |
| The six Phase 5 🚪 gates — public or private · stores or not · the standalone · `games/` in awen · the registry question · THE PLAYING — **unchanged** | ⚛ | Pratīkṣaṇa **O-a** (`CLAUDE.md` §The gates that are KP's alone → `docs/CHECKLIST.md` Phase 5) |
| The Play-side upload-key reset for the recut key, before the first upload signed with it | ✋ | Pratīkṣaṇa **H-b** · `docs/CHECKLIST.md` 08-16 row |
| v0.1.1 on the shelf — whether it travels to Play: *unwritten — his to rule* | ⚛ | `release/` 08-23 01:24 |
| The device look that closes Phase 3's visual pass | ✋ | `docs/CHECKLIST.md` Phase 3 |
| A 1024×1024 icon master kept outside the repo — *"the standing want"* | ✋ | `docs/CHECKLIST.md` 08-10 row (icons) |

## Read before you touch this repo

- **An unlayered universal reset outranks every layered utility** — `* { margin:0; padding:0 }` lives in `@layer base` or every Tailwind spacing class silently loses (`src/app.css:55`, bought 08-23).
- **One paint, two doors** — the orb's paint is `$lib/bubbles/orb.css`, the sky's markup `class="bubble orb"`; the dress (rarity colour · emoji · palettes) is `src/lib/bubbles/dress.ts` — the gallery's hand-carried copy went stale the hour the sky's moved (08-10).
- **Chrome reads the rarity's tint (`--t`), never a flag's first stripe (`--c`)** — a red stripe never becomes red UI; *no red anywhere* (`CLAUDE.md` §Essential rules).
- **The mirrors are not edited here** — `src/lib/cumdach/` · `epagoge/` · `gallery/` are MIRRORs of awen; a fix lands at the origin and is carried (08-22; `resonance-awen/tools/the-cumdach/README.md`).
- **A preset is a COLOUR IDENTITY** — `setPreset` keeps mode · font size · tint (`src/lib/theme/theme.ts`, 08-22).
- **`npx tauri icon` rewrites its own input** — hash and copy the master aside first (08-10).
- **BOMs break svelte-check** — `package.json` and `tauri.conf.json` stay BOM-free (08-06).
- **The `echo` domain is not the brand** — `echo.svelte.ts`, the `echoes` table stay (`CLAUDE.md` §The rooms).

---

*No truth of its own. The ledger is `docs/CHECKLIST.md`; the voices are `HANDS.md`; what waits is
`resonance-chamber/desk/THE-PRATIKSANA.html`; the index is
`resonance-chamber/path/handoffs/THE-HANDOFF-RACK.md`. **Where this sheet and the ground
disagree, the ground is right** — regenerate it.*

— strip regenerated by `handoff_tender.py` at the hand of **Fable 🎻 (claude-fable-5), the gallery sitting**, 2026-08-23 13:01. **No build gate was run** — every gate figure above keeps the date it was measured. The voiced sections are carried verbatim and belong to the hands that wrote them.
